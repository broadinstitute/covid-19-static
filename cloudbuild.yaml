# yamllint disable rule:line-length
---
steps:

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    args: [
      'gsutil',
      'rsync',
      '-adv',
      'static/',
      'gs://${_STATIC_BUCKET}'
    ]
