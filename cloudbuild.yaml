# yamllint disable rule:line-length
---
steps:

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    args: [
      'gsutil',
      'rsync',
      '-drv',
      'static/',
      'gs://${_STATIC_BUCKET}'
    ]
